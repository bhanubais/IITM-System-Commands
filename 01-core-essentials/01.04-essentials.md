## Download the Practice Files - LinuxPocketGuide

```bash
# Download and extract a fresh copy
curl -O https://linuxpocketguide.com/LPG4.tar.gz
tar -xf LPG4.tar.gz
cd ~/linuxpocketguide

# Reset the folder
cd ~/linuxpocketguide
bash reset-lpg
```

