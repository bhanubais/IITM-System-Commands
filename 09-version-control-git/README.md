# 09. Version Control and Git

Track source history, isolate work, and recover from broken changes.

## Command List

`git init`, `git status`, `git add`, `git commit`, `git branch`, `git checkout`, `git switch`, `git remote`, `git pull`, `git push`, `git merge`, `git log`.

## File Map

* [09.01 Git Fundamentals](09.01-git-fundamentals.md)

## Exam Targets

* Explain working tree, staging area, commit, branch, and remote.
* Build the standard add, commit, push path.

## Production Targets

* Inspect state before deployment.
* isolate risky changes on a branch.
* Review history before repair.

